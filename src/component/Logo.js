import React from 'react';
import Logokasa from "../img/logo.png";

const Logo = () => {
    return (
        <div>
            <img src={Logokasa} alt="logo" />
        </div>
    );
};

export default Logo;