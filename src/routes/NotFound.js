import Logo from '../component/Logo';
import MenuNav from '../component/MenuNav';

const NotFound = () => {
    return (
        <div>
            <Logo/>

            <h1>404 not found
            </h1>
            <MenuNav />
        </div>
    );
};

export default NotFound;